# Stage1 一次性实验配置归档

本目录归档不再作为主入口维护的 Stage1 小规模对照 YAML。

归档原因：

- `469/492/321/all1000` 等配置是一次性小样本 SFT/DFT 对照。
- 这类实验后续应通过统一 CLI、MLflow 参数或少量基准 YAML 管理，不再为每个样本数新建一个顶层 YAML。
- 若需要复现实验，可从本目录临时指定配置路径运行；默认文档和主流程不再引用这些文件。

保留在 `configs/stage1/` 的配置应代表稳定数据源或当前仍可复用的训练入口。
