base_model_name: /home/fsw/.cache/huggingface/hub/models--Qwen--Qwen2.5-Math-1.5B/snapshots/4a83ca6e4526a4f2da3aa259ec36c259f66b2ab2
model_name_or_path: outputs/stage1_mix_long_sft/checkpoint-300
train_dataset: data/grpo/train.jsonl
output_dir: outputs/grpo_cheap
reward_config: configs/grpo/reward.yaml
seed: 42
max_train_samples: 500
train_subset_seed: 42
train_subset_mode: fixed_random #head
compute_dtype: bfloat16
max_prompt_length: 768
max_completion_length: 768

learning_rate: 1.0e-6
epochs: 1.0
batch_size: 4
gradient_accumulation_steps: 4
warmup_ratio: 0.03
weight_decay: 0.01

logging_steps: 5
save_strategy: steps
save_steps: 25
save_total_limit: 3

load_in_4bit: false
lora_rank: 16
lora_alpha: 32
lora_dropout: 0.0

num_generations: 4
temperature: 0.8
top_p: 0.95

loss_type: dr_grpo
epsilon: 0.2
beta: 0.0
scale_rewards: false
importance_sampling_level: sequence
mask_truncated_completions: true

use_vllm: true
vllm_mode: colocate
vllm_gpu_memory_utilization: 0.4
vllm_tensor_parallel_size: 1
report_to: wandb
wandb_project: post-train-grpo
wandb_smoothing_window: 5
