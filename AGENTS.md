# 项目名称

Qwen 数学后训练项目

## 语言与输出

- 始终使用中文回答。
- 始终使用中文编写代码注释。
- 回答保持简洁、结构化；无特殊需要时不写长篇解释。

## 开发环境

- 优先使用 `.venv/bin/python` 运行和检查代码。
- 若 `.venv` 不可用，使用 `uv run python`。
- 除非用户明确要求，禁止重建环境或重新安装依赖。
- 除非用户明确要求，禁止下载大型模型或数据。

## 文档分工

- `AGENTS.md`：稳定规则，只放长期约束。
- `docs/SPEC.md`：当前阶段设计，只放本阶段目标、边界与口径。
- `docs/ARCHITECTURE.md`：系统分层、主链路与模块职责。
- `docs/STATE.md`：当前有效基线、已知问题、下一步重点。

## 文档优先级

- 稳定规则以 `AGENTS.md` 为准。
- 当前阶段设计以 `docs/SPEC.md` 为准。
- 结构分层以 `docs/ARCHITECTURE.md` 为准。
- 当前状态以 `docs/STATE.md` 为准。
- 若文档与实现冲突，优先修正文档并说明原因。

## 代码与设计原则

- 优先最小修改，禁止无关重构。
- 不修改与当前任务无关的文件。
- 遵循 KISS 与 YAGNI，不实现当前不需要的能力。
- `scripts/` 只允许放编排入口、参数解析、日志打印。
- `src/<package>/` 放训练、评测、数据处理、schema 与工具逻辑。
- 严禁把核心业务逻辑直接堆进 `scripts/`。
- 共享逻辑只保留一份实现，禁止平行复制。

## 文件组织准则

- 目录优先按稳定工作流拆分，不按临时实验状态拆分。
- 同类文件一旦需要靠很长文件名区分，就应升一级目录，而不是继续平铺。
- 评测与训练产物默认按人检索时的第一维分目录；当前优先按模型路径归档。
- 重构文件组织时优先收口路径规则与默认输出位置，避免路径散落在代码、测试和文档里。

## 训练与评测规则

- 统一尾部答案格式要求：必须包含 `\boxed{...}`。
- 答案提取与等价判定统一使用本地 parser + `math-verify`。
- 正式评测入口统一为 `scripts/eval_model.py`。
- 若旧实现与当前目标冲突，优先删除或替换，不为兼容而保留。

## 调试与验证规则

- 出现错误时必须先定位问题原因，再提出修复方案，最后修改代码。
- parser、`math-verify` 包装、数据 schema、评测统计必须可测试。
- 数据准备、SFT 训练、评测编排至少要有最小 smoke test 或 contract test。
- 没有新鲜验证证据时，禁止声称链路可用、修复完成或结果可信。
- 若改动影响训练、评测或数据格式，必须同步更新文档与相关测试。

## 完成定义

一次实现变更只有在满足以下条件时才算完成：

1. 代码、配置、脚本与文档口径一致。
2. 已运行相关最小验证命令并记录结果。
3. `scripts/` 未承载核心业务逻辑。
4. 未引入重复实现。

## Superpowers 约定

本项目默认禁用大多数 superpowers workflow。

默认仅允许以下 skill：
- `verification-before-completion`
- `systematic-debugging`
- `requesting-code-review`

其余 skill 默认不使用，除非用户明确要求。
若 skill 规则与本项目工作方式冲突，以本文件为准。

## Full Access 安全约束

- 即使开启 full access，未经用户明确要求，禁止执行删除性命令，包括 `rm`、`find -delete`、`git clean -fd`、`git reset --hard`。
- 禁止修改、移动或删除项目目录之外的文件。
- 禁止修改以下路径：`~/.ssh`、`~/.codex`、`/etc`、`/usr`、`/var`、`/opt`。
- 需要清理文件时，默认移动到项目内归档目录，不直接永久删除。
- 若写操作涉及项目外路径，必须先说明将要修改的绝对路径和目的。
- 未经用户明确要求，禁止修改 shell 配置、git 全局配置、SSH 配置和系统环境变量。

## TDD 规则

1. 本项目为探索项目，禁止重度 TDD。
2. 禁止对可变参数进行相等检验。
